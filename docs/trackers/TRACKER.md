# IRiS - Feature Tracker

## Status Legend
- `Working`
- `Mock`
- `Partial`
- `Not started`

> Tracker reset to the actual repository state on 2026-05-05. The repo was greenfield at build start, so this file reflects the code that exists in `frontend/` and `backend/`, not earlier documentation-only assumptions.

---

## Phase 0-1: Foundation + Auth
| Feature | Status | Notes |
|---------|--------|-------|
| Frontend scaffold (Vite + React + TS) | Working | `frontend/` created |
| Backend scaffold (FastAPI + SQLAlchemy) | Working | `backend/` created |
| Tailwind + design token foundation | Working | Colors, spacing, shared utility classes, and shell layout tokens added |
| Root health endpoint | Working | `GET /` returns `{"status":"ok","version":"1.0.0"}` |
| Deployment API routing | Working | Backend routers mount under `/iris/api/v1` by default, frontend production backend URL defaults to `http://d3sok4f0t46eww.cloudfront.net/iris` and appends `/api/v1` for requests, and backend CORS allows all origins/methods/headers |
| Python 3.10 backend startup compatibility | Working | Backend services now use `timezone.utc` under the existing `UTC` alias instead of importing Python 3.11-only `datetime.UTC` |
| JSON BOM compatibility | Working | Shared backend JSON-backed stores now use UTF-8 BOM-safe decoding so both seed/bootstrap startup and claims override-store reads succeed even when files are saved with an editor-added BOM |
| Login with email/password | Working | Seeded demo users authenticate, persist JWT sessions, and only redirect into the workspace after the restored session is revalidated; the login form no longer pre-fills the password field, and the seeded shared demo password is now `admin@2026` |
| Mock SSO button | Mock | Returns seeded `super_admin` session |
| `GET /iris/api/v1/auth/me` | Working | JWT decode and user lookup |
| JWT-protected app routes | Working | ProtectedRoute now validates persisted sessions through `GET /iris/api/v1/auth/me` before rendering protected pages, and frontend `401` responses clear stale auth back to `/login` |
| Logout | Working | Clears persisted auth state |
| User registration UI | Not started | Backend endpoint exists, no UI yet |
| MFA | Not started | Future phase |
| Real SSO / SAML | Not started | Future phase |

---

## Phase 2: App Shell + Navigation
| Feature | Status | Notes |
|---------|--------|-------|
| Sidebar layout | Working | Role-aware sections, collapse toggle, PNG-backed MetLife logo, blue/teal themed sidebar scrollbar, Phase 16 sidebar labels/items, and the underwriter `Contract Management` nav entry are implemented; the redundant claims-ops `Operations` sidebar link was removed |
| Topbar layout | Working | Environment banner, role switcher, user/logout |
| Super admin role switcher | Working | Drives role-aware dashboard/worklist payloads without limiting route access |
| Protected route skeleton | Working | All phase routes registered |
| Later-phase route placeholders | Working | Cedants, Contracts, Population, Cession Files, Settlements, Calculation Engine, Compliance Sanctions, Audit, Reports, and Admin routes are now implemented |
| IRiS chatbot | Working | Floating `IRiS Assist` drawer, Phase 16 header/copy updates, Phase 17 admin/compliance role-specific quick-action chips, route-aware location chip, expanded module navigation permissions, audit quick-action lookups seeded from `audit_events`, safe Markdown response rendering, and `/iris/api/v1/chatbot/message` are implemented; the drawer is now slightly wider for better readability, the opening/help copy is more direct and professional, and live responses are prompted to return compact Markdown with crisp paragraphs or flat bullets when helpful. The backend now uses the shared OpenAI client from `.env` to plan read-only SQL against the live runtime tables, execute the generated queries, repair invalid SQL once when needed, and answer from returned database context instead of the earlier hybrid hardcoded intent routing. The OpenAI client bootstrap now retries from the installed runtime package instead of relying on a stale import-time singleton, and settlement pipeline questions now read fee/net reconciliation values from `cession_file_records.mapped_data` JSON via catalog hints so fee deductions are reflected in live answers. Multi-row Settlement-file questions now explicitly aggregate uploaded row totals once per file and use the linked `settlements` row for the authoritative IRiS system total so repeated row-level diagnostic payloads are not multiplied into inflated system net answers. The backend now also blocks off-topic questions and PII-seeking prompts before SQL planning, returning explicit refusals that the assistant cannot help with irrelevant questions and does not have access to PII |

---

## Phase 3: Dashboard
| Feature | Status | Notes |
|---------|--------|-------|
| Role-aware dashboard page | Working | Admin / Underwriter / Claims Ops / Compliance views implemented |
| KPI cards | Mock | Loaded from backend JSON by role |
| Graph sets | Mock | Loaded from backend JSON by role, with screenshot-backed two-row dashboard visual sections across admin, underwriter, claims ops, and compliance, including the Phase 17 underwriter `Population Movement` and `Renewal Pipeline` charts plus the claims-ops `Cedant File Delivery (30d)` chart |
| IRiS Insight banner | Mock | Now derived from `intelligence_feeds_complete.json` instead of static per-role copy |
| Recent Activities workspace | Mock | Screenshot-backed role-aware dashboard activity workspace now replaces the old intelligence-card section, with Team Activities / IRiS AI / Escalations tabs and backend JSON feeds for admin, underwriter, claims ops, and compliance |
| Quick action buttons | Working | Wired to navigation placeholders |
| Integration Health panel | Mock | Screenshot-backed admin second-row panel rendered from dashboard JSON |
| Pending Admin Approvals panel | Mock | Screenshot-backed admin approvals panel rendered from dashboard JSON |
| High-Risk Cedants panel | Mock | Screenshot-backed underwriter panel rendered from dashboard JSON |
| High-Impact Exceptions panel | Mock | Screenshot-backed claims-ops panel rendered from dashboard JSON |
| Active Screening Hits / Audit Heatmap | Mock | Screenshot-backed compliance second-row dashboard panels rendered from dashboard JSON |

---

## Phase 4: Worklist
| Feature | Status | Notes |
|---------|--------|-------|
| Claims Ops worklist (live DB) | Working | Live status for `WL-9202`, `WL-9204`, and `WL-9206` now overlays the shared screenshot-backed mock register so the richer card layout and cross-role accessible list stay consistent; newly created DB worklist rows that are not part of the seed register are appended into the queue payload so settlement-generated approval and sanctions tasks appear immediately and resolve through `/worklist/:wlId` links |
| Admin worklist | Mock | Admin now reads the shared 13-card screenshot-backed mock register with role-aware `Read-only` tagging instead of the earlier narrow admin-only mock subset |
| Underwriter worklist | Mock | Underwriter now reads the shared 13-card screenshot-backed mock register with role-aware `Read-only` tagging and owner/entity/impact metadata |
| Compliance worklist | Mock | Compliance now reads the shared 13-card screenshot-backed mock register including `WL-9201`, `WL-9203`, `WL-9208`, `WL-9214`, and the cross-team read-only cards, plus runtime sanctions-review overlays for newly persisted ad-hoc screening cases that are still pending compliance disposition |
| Summary tiles | Working | DB-backed for Claims Ops, mock for other roles |
| Search/filter | Working | Client-side filters now cover My Tasks / Team Tasks / All Accessible plus priority / status / category / source, with screenshot-style bordered controls |
| Quick filter pills | Working | Overdue / Approval / AI / Hold / High Impact |
| Grid/List view toggle | Working | Both layouts implemented, with the grid tightened to the screenshot-backed compact 3-column density; worklist cards now use denser spacing, a single readable owner row, and assignee name resolution from the user store when available with email-handle fallback for mock rows that do not have a matching user record |
| Claims Ops polling | Working | 30s refresh in UI |
| Worklist PATCH endpoint | Working | Backend status update route |
| Expanded card detail | Partial | Full-page worklist detail route is implemented for the screenshot-backed revised Phase 8 operations flow at `/worklist/:wlId`; broader role-specific detail experiences remain pending |
| Saved views | Not started | Future phase |

---

## Underwriting
| Feature | Status | Notes |
|---------|--------|-------|
| Cedants list page | Working | Search, status filter, screenshot-style register table, and `+ New Cedant` action implemented; the live register now removes the stale duplicate onboarding row for `CED-1202`, and the Country column renders only the ISO country code text without the earlier prefixed flag glyph/fallback code |
| Cedant detail page | Partial | All 13 master-data sections plus linked operations sections are rendered; the screenshot-backed `Operations` rail now includes `Mapped Contracts`, `Calculations`, `Sanction Screening`, and `Audit Trails`, the cedant calculations workspace is populated from seeded contract performance data with a Northstar screenshot override for the calculation tables/KPIs, the Sanction Screening linked section follows the reference screenshot with KPI cards, source status cards, all-source screening CTA, filters, and detailed history columns, and the backend normalizes that source-card payload to exactly the two screenshot-backed providers (`OFAC` and `FinCEN`) so legacy duplicate FinCEN cards no longer render for specific cedents |
| Cedant onboarding wizard | Partial | Full 13-step UI flow implemented with stepwise persistence and AI intake; sanction screening now runs against the saved OFAC / FinCEN cache for new cedents and automatically advances to Audit & Approval when no potential match is found; approval worklist is created on record creation because the spec does not define a separate final-submit endpoint |
| Cedant section APIs | Working | List, detail, create, section patch, status update, and sanction history routes implemented |
| Cedant AI extract | Mock | Deterministic mock extraction response, wired into the wizard upload flow |
| Cedant sanction screening | Working | `POST /underwriting/cedents/{cedent_id}/sanction-screening` now delegates to the compliance sanctions engine, persists a screening case, evaluates OFAC / FinCEN cache rows, updates cedent screening status, and appends per-source history rows; potential matches remain pending for human approval while cleared all-source runs allow onboarding to continue. Exact DB-backed watchlist name hits with aligned jurisdiction are now held in pending review instead of being auto-cleared as false positives when registration/address data is sparse. Underwriting audit persistence now allocates the next daily `audit_id` from the highest existing suffix and retries once on unique-key collisions so repeated sanction-screening runs do not fail with `audit_events.audit_id` conflicts in the local demo DB |
| Screenshot-only cedant sub-sections | Partial | Pension Scheme, Key Contacts, and other screenshot-only detail sections persist through a mock JSON overlay because `docs/SCHEMA.md` does not define backing tables/columns |
| Contracts list page | Working | Screenshot-style register table, `+ New Contract`, and row-level view/members/amend actions implemented, with the actions cell now locked to a single inline row like the reference UI |
| Contract detail page | Partial | The contract detail workspace now follows the cedent-style vertical `SectionPanel` shell from the screenshots instead of the earlier horizontal tab strip, with the same contract payload preserved and redistributed across the spec-backed left-nav sections (`Master Data` through `Audit & Compliance`); the shared section rail is now sticky on desktop with its own scroll window so the navigation stays visible below the fixed header while long contract sections scroll, the nav scrollbar is now slimmer and only appears on hover, and the duplicate `Latest Settlement Snapshot` card has been removed from `Details & Performance`. Settlement-linked header actions, enriched overview intelligence, `audit_events`-backed Audit Log/Compliance Trail reads, the read-only shared `Clauses` catalog, rolled-forward mock settlement histories, descending settlement tracker ordering with pending-row highlighting, Maple Leaf `CAD` currency normalization, the 8,900-life Maple sample footer, and the most-recent-paid `Latest Net Payout` logic all remain in place. Several screenshot-only fields remain mock-backed because the SQLAlchemy layer does not yet model the full contract child tables from `docs/SCHEMA.md`. |
| Contract section APIs | Working | List, detail, create, section patch, amendment, performance, calculations, member list, and upload-members routes implemented |
| Contract calculations | Mock | Calculator responses are deterministic and driven from the contract performance overlay until the later claims/calculation-engine phases provide live processing inputs |
| Contract member list | Working | Contract Detail now reads live current `policy_register` rows when population exists for the contract, including live summary counts and row-level status updates; Maple Leaf (`LSC-2024-044`) intentionally stays on a documented mock sample for this tab because the seeded population only contains 4 demo rows while the contract financials reference 8,900 covered lives |
| Contract member upload | Working | `POST /underwriting/contracts/{contract_id}/upload-members` now imports baseline population CSV or Excel (`.xlsx`) rows into `policy_register` with SCD2 upserts and audit logging; the live upload path currently tolerates missing/invalid `date_of_birth` and `annual_pension` by reusing current member values when present and defaulting new members to placeholder values until the later enrichment phase |
| Screenshot-only contract sub-sections | Partial | Reference Pool, Actuarial Basis, Risk & Limits, Operational Terms, Audit & Compliance, and richer performance/member data persist through a documented mock JSON overlay because the current repo does not yet implement the full schema tables/models |
| Population module | Working | Policy-register-backed Population page, cascading cedent/contract filters, member history drawer, and defer endpoint are implemented |
| Population upload handoff | Working | The Population page CTA now opens the cedant/contract cascade modal and submits baseline population CSV or Excel (`.xlsx`) files through `POST /underwriting/contracts/{contract_id}/upload-members`; Claims > Cession Files remains the Pension Status processing path |
| Population `last_verified` field | Mock | The UI/API spec requires `last_verified`, but `docs/SCHEMA.md` does not define a backing column on `policy_register`, so it is served from seed/override metadata instead of invented schema |

---

## Claims
| Feature | Status | Notes |
|---------|--------|-------|
| Cession file processing | Partial | Queue page now supports the screenshot-backed full-page upload/history workflow at `/claims/cession-files/new` and `/claims/cession-files/:fileId`; upload stores source content and waits for confirmed detection/mapping before creating validation artifacts; the processing route now clubs detection and contract mapping into one combined review step, moves the Clauses view after Anomalies and Resolutions without rewinding backend file state, and keeps reopened legacy files compatible by auto-hydrating the validate step when a record was parked at the old pre-validation clauses stage; cedent/contract detection now prioritizes strong filename matches, uses OpenAI fallback for low-confidence/conflicted matches when configured, clears downstream records/exceptions when overrides change, Pension Status CSV or Excel (`.xlsx`) files validate against confirmed-contract `policy_register` rows before applying SCD2 population updates, and Settlement files are detected by the required settlement header signature, including tab-delimited CSV aliases such as `Applicable Indexation / Escalation`, `Fee (Admin)`, `Settlement Period`, `Fixed Amount`, and `Floating Amount`, plus pipe-delimited `.txt` uploads carrying the same normalized headers, before exact fixed/floating/net reconciliation; Settlement validation now surfaces settlement-only repair suggestions in the Anomalies and Resolutions flow for header remapping, ISO date normalization, mode-based date/text imputation, and currency/amount cleanup, while the Resolutions screen starts with no preselected action, removes the old reference column in favor of a wider action area, keeps Override/Manual available when needed, and the persisted Maple settlement record blobs now normalize `CA$` text to `CAD`; the first detect/map run now lands on `Anomalies` before `Resolutions`, even when settlement warnings are present; the Clauses step now reads from the shared hardcoded contract clause catalog for Settlement uploads and shows only the `Settlement` category rows; accepted or overridden Settlement resolutions update the processed record state used by downstream reconciliation; Settlement processing now aggregates normalized incoming fixed leg, floating leg, fee, interest, and net values across all file rows, uses the pending `settlements` table row for cedent/contract/quarter as ground truth when present, falls back to the same contract-performance quarter values shown on the contract detail financial card before any mock baseline is used, treats admin fee as a settlement deduction when recomputing net, routes mismatches to reconciliation review, and on exact match creates the Settlement Pending worklist item plus a persisted `cession_file` sanctions case; the cession sanctions card now stays visible on its own `Sanction Screening` tab even when the case auto-clears, removes the compliance assignment in those auto-cleared paths, shows combined raw-watchlist and IRiS AI findings when a raw hit exists, and only creates/retains the compliance worklist assignment with a `Pending` card state when the raw match is still upheld by IRiS for analyst review; generated worklist rows resolve an assigned person name from the seeded users table, render dynamic processing-worklist status pills (`Pending`, `Auto-Cleared`, `Escalated`) from the linked settlement/sanctions outcome, expose direct hyperlinks into the corresponding Settlement register row or Sanctions case while still retaining worklist detail access, and the settlement pipeline view now shows the screening task on its own `Sanction Screening` tab while leaving only the settlement item on the `Worklist` tab; that sanctions tab now foregrounds whether the cedent was auto-cleared, shows OFAC and FinCEN provider outputs, surfaces the IRiS analysis with confidence, adds explicit pending-status plus assigned-compliance HITL ownership when analyst review is still required, and now uses a decision-first settlement-screening layout that removes repeated auto-clear labeling, the redundant task-context panel, and the release-readiness card, aligns OFAC / FinCEN / Raw Match into one row, keeps IRiS Analysis as a full-width next-row narrative card, and makes the linked sanctions case ID itself clickable into `/compliance/sanctions/{screening_ref}`; the exact-match path now also creates a new pipeline settlement row in the Settlements register using a cession-file-specific settlement ID while preserving the pending baseline row for future reconciliation lookups, and persists a real `cession_file` sanctions case into the Sanctions workspace; the post-process step bar now keeps the Summary / Sanction Screening / Files / Worklist / Audit completion ticks aligned to the visible flow, and the `Files` step no longer shows as completed when a failed settlement run produced no downstream artifacts; exact-match downstream cash tracker/GRDR files are generated into a pipeline `Files` step for view/download before the user pushes them to SFTP/Reports; settlement expectations skip incomplete sample `policy_register` populations and use the documented deterministic mock fallback only after the contract-performance alignment path is exhausted; richer audit presentation and contract-period expectations remain partly mock-backed where schema-tracked data is unavailable. Non-settlement pipeline headers are now explicitly labeled `(beta)` while settlement retains the standard header copy, visible mojibake in settlement-reconciliation subtitles, clause references, audit placeholders, and validation breadcrumbs is normalized across the live claims flow, and cession validation audit events now render `Validation Engine` as `AI Agent` for both historical seeded sessions and new uploads. Claims audit persistence now allocates the next daily `audit_id` from the highest existing suffix instead of `COUNT(*)`, preventing duplicate `audit_events.audit_id` collisions in local demo databases when the exception-processing step resumes after earlier cleanup or gaps. The upload step quick-access buttons now resolve from the live contents of `backend/testcases`, replacing the older hardcoded sample names while keeping testers on the updated backend file set, and each quick-access testcase card now selects on card click while exposing only a separate download control that does not trigger selection. The queue query now uses chronological descending order with a descending `file_id` tiebreaker, and the local demo DB was cleaned twice: first to remove the newest Maple Leaf block at the top of the queue, then to remove every `cession_files` entry dated `2026-05-11` or later together with linked mock/runtime artifacts. The post-upload experience now stays inside the existing stepper by inserting a dedicated `Workflow` orchestration step between `Upload` and `Detect & Map`, adding Phosphor icons across the full horizontal stepper, using a live orchestration hub plus full-width agent accordions for monitoring/HITL/skips, and gating settlement reconciliation output to the `Process` agent so processing results do not surface during anomaly review. The JSON-backed cession and settlement override stores now recover from trailing-content corruption and use locked atomic writes so concurrent workflow updates no longer 500 the cession detail flow. |
| Cession file queue metrics | Mock | Top metrics and throughput cards follow the API/spec baseline and layer live queue rows on top of the seeded mock counts |
| Cession file detect/map/clauses/validate stages | Working | Detect, map-contract, clauses, validate, process-exceptions, process, approve, summary, and pipeline-status endpoints are wired end to end and frontend-connected; Pension Status detection/manual override, Settlement detection/manual override, DB-derived contract clauses, the shared hardcoded Settlement clause catalog, pandas validation, active-member coverage checks, settlement reconciliation summary/worklist routing, data-validation exception creation, and processing now use the confirmed uploaded CSV or Excel (`.xlsx`) file plus current DB/settlement rows instead of pre-confirmation sample rows |
| Cession file summary/worklist/audit detail | Mock | The routed cession-file workflow summary/worklist/audit sections remain deterministic mock/seed payloads where the schema has no backing tables for settlement impact, commentary, or richer workflow session state; the audit section reads seeded `audit_events` rows when available |
| Settlements | Working | Screenshot-backed settlement register now uses the 7-card `Settlement & Reconciliation` summary strip, 3-chart analytics row, compact search/status/list-grid toolbar, latest-updated settlement ordering so newly created cession settlement cases appear first, and the current baseline register is aligned to `Q1 2026` for Northstar/Helvetia/Maple/Bavarian/Atlas so incoming `2026 Q1` settlement uploads compare against the current seeded/live register; exact-match settlement-file processing now appends a new page-level settlement record for the processed cession file instead of only updating worklist state, while keeping the baseline pending row available for future quarter-ground-truth reconciliation; export/statement actions, the right-side detail panel, approve/hold/dispute actions, and `audit_events`-backed detail-panel audit trails remain in place |
| Calculation engine | Partial | Live contract selector and backend calculation run endpoint are implemented; screenshot-only save/submit workflow and richer audit context are deterministic UI-local mocks because the current phase spec only defines contracts list + run |

---

## Compliance
| Feature | Status | Notes |
|---------|--------|-------|
| Sanctions screening page | Working | The compliance sanctions module now follows the screenshot-backed sanctions case workspace instead of the earlier dashboard clone: `/compliance/sanctions` renders the 7-card KPI strip, ad-hoc screening form, selected OFAC / FinCEN source chips, case tabs/search/filter, and cases table, while `/compliance/sanctions/:screeningRef` renders the report/detail flow; persisted `screening_events` seed the case list, `GET /sanctions/overview` serves the workspace payload plus workbook metadata for source selection, `GET /sanctions/hits/{screening_ref}` returns the report payload, `PATCH /sanctions/hits/{screening_ref}` updates dispositions, and `GET /sanctions/screen` reads the selected OFAC / FinCEN screening cache payload from `screening_cache_lists` with a seed fallback before OpenAI/fallback verification; newly persisted ad-hoc cases store all screened sources so the result page shows OFAC and FinCEN when both were selected, and settlement exact-match processing now persists `cession_file` screening cases so those entries also appear in the sanctions workspace. Exact-name cache matches with non-conflicting jurisdiction now default to conservative pending-review handling before the generic false-positive heuristic, and the report-level `Network Analysis`, `Decision History`, and `Adverse Media` insights now render inside a switchable tab workspace with production-style copy, quarterly 2025 history for active cedents, pre-engagement-only history for onboarding cedents, and richer adverse-media context including last review date and sources checked |
| Screening cache workbooks | Working | `/compliance/screening-cache` is now the third Compliance page and hosts editable OFAC / FinCEN workbook grids generated from the current screening cache; `GET /sanctions/cache-workbooks/{list_name}/download` exports the current cache as editable `.xlsx`, `PATCH /sanctions/cache-workbooks/{list_name}` saves workbook edits back to `screening_cache_lists`, and the repository now includes the get/update methods required by the save path. The current UI labels this workspace as `Screening Database (Demo)` and renders the OFAC / FinCEN editors as tabs so the active workbook can use the full page width. Workbook list/header row counts now prefer the actual editable entry count when workbook entries exist, preventing stale persisted aggregate counts such as the older FinCEN `4122` value from leaking into the UI |
| Audit & Traceability workspace | Working | Phase 13b is implemented at `/compliance/audit` with the screenshot-backed 10-item left nav, dashboard/search/risk-governance/data-access/reporting sections, role-gated `/iris/api/v1/audit/*` endpoints, and mock-backed export downloads; the additions/build-plan text says 9 sub-sections, but the UI spec and screenshot include Export Audit Reports as a 10th item, so the screenshot-backed navigation was used |
| Cedant-level screening | Partial | Implemented inside Cedant Detail and the onboarding wizard; the backend single-entity watchlist verification path is now OpenAI-backed with deterministic fallback, while the broader onboarding/detail UX remains partial/mock-backed |

---

## Reports
| Feature | Status | Notes |
|---------|--------|-------|
| Report catalog + detail workspace | Working | Phase 14 is implemented with `/reports` and `/reports/:reportId`, a DB-seeded report catalog, role-filtered backend access, screenshot-backed category rail/filter panel/export actions, the tighter bordered reports-catalog visual treatment from the source screenshots, static mock detail previews, `POST /reports/export` for csv/excel/pdf/zip downloads, and generated Settlement cession output files rendered under the `Settlement Reports` category with `Operations` as the table category and row-level popup table view/download actions; the source docs fully specify 21 report rows, while 2 Debugging report definitions are missing, so the Debugging category is rendered with `0` rather than inventing undocumented entries |

---

## Admin
| Feature | Status | Notes |
|---------|--------|-------|
| Admin workflow screens | Working | Phase 15 is complete: `/admin/users` and `/admin/library` are implemented with screenshot-aligned tabs, modals, drawers, and sidebar routing; users flow reads/writes the `users` table, permissions and approval matrix remain deliberate static POC responses per the spec, access logs now read from seeded `audit_events`, and the reference library now reads from seeded `reference_data_versions` plus `screening_cache_lists` tables. Administration now also includes a dedicated `/admin/workflow-agents` workspace plus sidebar entry for configuring per-agent enablement, confidence thresholds, HITL behavior, escalation rules, retry limits, and fallback modes for the autonomous cession workflow. |
| User management UI | Working | Users list, create invite, role/status edit, revoke flow, temp-password response, and the six Phase 15 seed users are aligned to `/iris/api/v1/admin/users*` |

---

## Global
| Feature | Status | Notes |
|---------|--------|-------|
| Desktop-first layout | Working | Shared shell and content density tuned for 1024px+ |
| Error boundaries on all pages | Working | Global app-level and route-resetting page-level React error boundaries now wrap login and protected workspace rendering, with branded retry/dashboard/refresh fallbacks |
| Loading skeletons throughout | Partial | Shared shimmer skeleton components are implemented and wired into dashboard, worklist, cedants, contracts, settlements, and admin users/library list states; some secondary detail panels and drawers still use plain loading copy |
| Toast notification system | Working | Global toast store + viewport are mounted app-wide and now handle action feedback across admin users/library, sanctions, population, calculation engine, and settlement workbench flows instead of page-local inline banners |
| Consistent empty states | Partial | Shared empty-state and table-row components now cover worklist, cedants, contracts, population, cession files, settlements, admin library, and shared DataTable consumers; some deeper detail tabs still retain older inline empty copy |
| Responsive mobile support | Not started | Out of current scope |
| Structured backend logging | Working | Service-layer info/debug/error logging added, including underwriting and claims services |
| Frontend production build | Working | `npm run build` passes |
| Backend smoke verification | Working | Health, auth, dashboard, worklist, underwriting cedants/contracts/population including contract detail V2 performance enrichments, claims cession-file queue/detail/upload/pipeline/summary, operations pipelines/detail/step/advance/abort/resolve including outcome-to-settlement approval, settlements/detail/approve/hold/dispute, calculation contracts/run, compliance sanctions overview/detail/trigger/bulk-screen/hits/resolve/screen including the sanctions case detail payload and OFAC / FinCEN cache-backed screen path, audit dashboard/search/financial-impact/approvals/ai-decisions/manual-overrides/reference-data/access-logs/document-history/export-reports/download, reports catalog/detail/export with role-filtering checks, admin users/permissions/approval-matrix/audit-log/library/upload/sync, and chatbot message endpoints verified |

---

## Next Build Unit
- No remaining strict-sequence build-plan phases remain after the completed Phase 17 Polish Pass; the open backlog is now the tracker-only residual scope such as registration UI, MFA, real SSO/SAML, responsive mobile support, and broader live OpenAI rollout across the remaining mock AI experiences.
